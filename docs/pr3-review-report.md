# PR3 통합 리뷰 리포트

## 결과
- 상태: **승인 (Approved)**

## 체크리스트
- 역할 분리
  - Core(`src/core/progression.js`)에 업그레이드/비용/DPS/생존력 계산을 순수 함수로 구현했고 Phaser 의존성 없음.
  - Scene(`src/scenes/UILayoutScene.js`)은 Core 함수 호출 및 UI 반영만 수행.
- 기능 완료
  - 공격력/체력 강화 버튼 각각 동작.
  - 골드 부족 시 구매 차단 및 메시지 표시.
  - 구매 성공 시 레벨/스탯/골드/다음 비용 즉시 갱신.
- 경제/체감
  - 레벨 증가에 따른 단조 증가 비용 적용.
  - DPS/생존 시간 지표 노출로 성장 체감 제공.
- 회귀 위험
  - 기존 자동전투 루프(tickCombat), 처치 보상, 사망 재정비 흐름은 유지됨.

## 발견 이슈
### 필수 수정
- 없음

### 권장 개선
1. 업그레이드 성공/실패 로그를 combatLog에도 기록하면 디버깅이 쉬워집니다.
2. 탭 설명 문구를 새 업그레이드 구조에 맞춰 정리하면 UX 일관성이 향상됩니다.
